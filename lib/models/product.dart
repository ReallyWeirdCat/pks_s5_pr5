class Product {
  final int id;
  final String title;
  final String description;
  final String photo;
  final double price;
  final int discount;

  Product({
        required this.id,
        required this.title,
        required this.description,
        required this.photo,
        required this.price,
        required this.discount
  });
}